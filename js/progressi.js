// progressi.js — schermata progressi: 5 bersagli, foto con sagoma di
// allineamento, confronto affiancato, volume settimanale per gruppo.
// Scheletro: solo il punto di innesto nella vista, nessuna logica ancora.

function renderProgressi(container) {
  container.innerHTML = `
    <p class="view-placeholder">I cinque bersagli e il confronto foto non sono ancora implementati.</p>
  `;
}

export { renderProgressi };
