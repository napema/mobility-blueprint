// assessment.js — Settimana 0: Test 1 (muscolo/nervo), Test 2 (lateralizzazione,
// da portare da asimmetria-bacino-protocollo.html), Test 3 (baseline 5 bersagli).
// Scheletro: solo il punto di innesto nella vista overlay, nessuna logica ancora.

function renderAssessment(container) {
  container.innerHTML = `
    <p class="view-placeholder">L'assessment guidato (Test 1, 2, 3) non è ancora implementato.</p>
  `;
}

export { renderAssessment };
